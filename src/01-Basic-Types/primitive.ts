const userName: string = "Tom";
const age: number = 25;
const isStudent: boolean = true;
const sym: symbol = Symbol("唯一标识");
const nullVal: null = null;
const undefinedVal: undefined = undefined;

console.log(userName, age, isStudent, sym, nullVal, undefinedVal);
